# 0.5 diagrama spa

```mermaid
sequenceDiagram
    participant Usuário
    participant Browser
    participant Servidor

    Usuário ->> Browser: Digita um input
    Browser ->> Browser: JavaScript armazena o input
    Usuário ->> Browser: Clica no input
    Browser ->> Servidor: JavaScript faz a requisição
    Servidor ->> Browser: Retorna o resultado
    Browser ->> Browser: Recebe o response
